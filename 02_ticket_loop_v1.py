# start of loop

# initialise loop so it runs at least once
name = ""
ticket_count = 0
max_tickets = 5

while name != "xxx" and ticket_count < max_tickets:

    # get details
    name = input("Name: ")
    ticket_count += 1
